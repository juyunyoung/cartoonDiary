version: 1.0
runtime: python3
build:
  commands:
    build:
      - pip install --upgrade pip
      - pip install -r requirements.txt --no-cache-dir
run:
  command: python main.py
  network:
    port: 8080
  env:
    - name: AWS_REGION
      value: us-east-1
